export enum Gender {
  MALE = 1,
  FEMALE = 2
}

export enum UserStatus {
  INACTIVE = 1,
  ACTIVE = 2
}

export enum IsAdmin {
  FALSE = 1,
  TRUE = 2
}

export enum UserType {
  STUDENT = 1,
  LECTURER = 2
}

export const USER_API = {
  ROOT: '/users'
};
